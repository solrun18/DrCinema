import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  trailerContainer: {
    marginTop: 20,
    width: 320,
    height: 230,
  },
  header: {
    fontSize: 30,
    fontWeight: 'bold',
    color: 'white',
    textAlign: 'center',
    marginTop: 30,
  }
});
