import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  list:{
    // borderColor: 'red',
    // borderWidth: 2,
  },
  text:{
    // fontSize: 15,
    // color: 'red',
    // borderColor: 'blue',
    // borderWidth: 2,
  },
});
